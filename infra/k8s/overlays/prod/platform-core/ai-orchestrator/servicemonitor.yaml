apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ai-orchestrator-sm
  namespace: platform-core
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: ai-orchestrator
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
