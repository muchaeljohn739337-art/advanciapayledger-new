apiVersion: v1
kind: Service
metadata:
  name: ai-orchestrator
  namespace: platform-core
spec:
  selector:
    app: ai-orchestrator
  ports:
    - name: http
      port: 4006
      targetPort: 4006
  type: ClusterIP
