apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: tenant-service-secrets
  namespace: platform-core
spec:
  refreshInterval: "1m"
  secretStoreRef:
    name: vault-backend
    kind: SecretStore
  target:
    name: tenant-service-secrets
  data:
    - secretKey: DATABASE_URL
      remoteRef:
        key: secret/data/advancia/tenant-service
        property: database_url
