apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: tenant-service-sm
  namespace: platform-core
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: tenant-service
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
