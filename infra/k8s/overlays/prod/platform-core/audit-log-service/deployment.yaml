apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-log-service
  namespace: platform-core
  labels:
    app: audit-log-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: audit-log-service
  template:
    metadata:
      labels:
        app: audit-log-service
    spec:
      serviceAccountName: audit-log-service-sa
      containers:
        - name: audit-log-service
          image: ghcr.io/advancia/audit-log-service:REPLACE_TAG
          ports:
            - containerPort: 4010
              name: http
          envFrom:
            - secretRef:
                name: audit-log-service-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 8
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
