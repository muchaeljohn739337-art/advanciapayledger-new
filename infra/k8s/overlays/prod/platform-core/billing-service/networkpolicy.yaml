apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: billing-service-policy
  namespace: platform-core
spec:
  podSelector:
    matchLabels:
      app: billing-service
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: api-gateway
      ports:
        - protocol: TCP
          port: 4003
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443
