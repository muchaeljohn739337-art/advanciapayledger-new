apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metering-service-sm
  namespace: services
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: metering-service
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
