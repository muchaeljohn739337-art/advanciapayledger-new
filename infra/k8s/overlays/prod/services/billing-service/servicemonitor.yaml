apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: billing-service-sm
  namespace: services
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: billing-service
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
