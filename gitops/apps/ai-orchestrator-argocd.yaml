apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ai-orchestrator
  namespace: argocd
  labels:
    app.kubernetes.io/name: ai-orchestrator
    app.kubernetes.io/part-of: advancia-services
  annotations:
    argocd-image-updater.argoproj.io/image-list: ai-orchestrator=032474760584.dkr.ecr.us-east-1.amazonaws.com/ai-orchestrator
    argocd-image-updater.argoproj.io/ai-orchestrator.update-strategy: semver
    argocd-image-updater.argoproj.io/ai-orchestrator.allow-tags: regexp:^v[0-9]+\.[0-9]+\.[0-9]+$
    argocd-image-updater.argoproj.io/ai-orchestrator.write-back-method: git
    argocd-image-updater.argoproj.io/write-back-target: kustomization
spec:
  project: default
  source:
    repoURL: "git@github.com:your-org/infra.git"
    targetRevision: HEAD
    path: "k8s/overlays/prod/services/ai-orchestrator"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: services
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
